<form method="post" action="{{ path('admin.playlists.delete', {'id': playlist.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer cette playlist?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ playlist.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
